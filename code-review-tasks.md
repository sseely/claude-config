# Self-Improvement Tasks — ~/.claude Configuration
<!-- Generated by /self-improve on 2026-07-01 (Opus 4.8, scoped delta run).
     ALL ITEMS APPLIED 2026-07-01 in the same session (see checkboxes).
     Verdict was REQUEST CHANGES (Critical=1, Warning=2); all now resolved.
     Kept as an applied-change record — safe to delete (git preserves history). -->

## Must fix (Critical)
- [x] `.gitignore` — added `daemon/`, `daemon.lock`, `daemon.log`,
  `daemon.status.json`, `jobs/`, `logs/`, `telemetry/`, `chrome/`,
  `.last-update-result.json`. `git check-ignore` confirms `daemon/control.key`
  (secret) and `telemetry/` (PII) are now ignored; none were previously tracked.

## Should fix (Warning)
- [x] `skills/self-improve/SKILL.md` — added `fable` alias and `claude-fable-5`
  ID to the Agent B pre-seeded list; added `opusplan[1m]`; noted `best`=Fable
  where available and that `claude-fable-5[1m]` is invalid (Fable is natively 1M).
- [x] `settings.json:142` — `claude-fable-5[1m]` → `claude-fable-5`. File still
  parses as valid JSON.

## Consider improving (Suggestion)
- [x] `FABLE-REACTIVATION.md` — removed via `git rm` (work landed in 0b38c9a;
  recoverable from commit 1e374fd).
- [x] `agents/04-quality-security/error-detective.md` — added
  `Full standard: ~/.claude/rules/diagnosis.md`, matching debugger.md.
- [x] `CLAUDE.md:59-61` — compressed Diagnosis section to a tighter pointer;
  3866B → 3790B (back under the 4KB cap with headroom).
- [x] `rules/parallelism.md` — added a note that Fable 5 auto-falls back to
  Opus 4.8 on its cybersecurity/biology safety classifiers.
- [x] `skills/sandbox/SKILL.md` — added a `sandbox.credentials` (v2.1.187)
  adoption note for defense-in-depth on `--dangerously-skip-permissions` runs.

## Inline comments to add (Notes)
- [x] settings.json hook-matcher — VERIFIED instead of commented (strict JSON
  cannot hold `//`): all matchers use documented forms (`Bash`,
  `Write|Edit|MultiEdit`); a v2.1.195 matcher fix does not affect valid matchers.
- [x] `rules/prompting-quality.md` — added an HTML-comment note that
  `paths:`-scoped rule loading must be verified as a feature before adoption.
