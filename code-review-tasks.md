# Self-Improvement Tasks — ~/.claude Configuration
<!-- Generated by /self-improve on 2026-07-24 (full 9-agent fan-out, Opus 4.8).
     Verdict: REQUEST CHANGES (Critical=1, Warning=15).
     Review each item, remove any you don't want, then run:
     /plan-mission implement the tasks in code-review-tasks.md
     NOTE: W0 (Opus 5) was discovered mid-run via user prompt, not by an agent —
     the same-day launch post-dated the agents' doc fetches. -->

## Must fix (Critical)
- [ ] `templates/autonomous-settings.json:56-57` — grants `safe_delete_symbol` +
  `rename_symbol` but omits `mcp__serena__find_referencing_symbols` (present in
  global settings). An autonomous refactor checks references before rename/delete
  → permission prompt → stalls with no human. Fix: add
  `mcp__serena__find_referencing_symbols`, `find_symbol`, `get_symbols_overview`
  to the template allow-list.

## Should fix (Warning)
- [ ] `rules/parallelism.md` model-routing table + `skills/self-improve/SKILL.md`
  pre-seeded model lists — STALE as of 2026-07-24: Opus 5 shipped today
  (~Fable-5 capability at ~half price, SOTA coding/knowledge, low/med/high effort
  toggle). Fix: refresh the routing table and pre-seeded lists for Opus 5;
  re-evaluate whether the `settings.json` session default should be `opus`
  (=Opus 5) vs `fable` given the new price/capability point.
- [ ] `rules/prompting-quality.md:35-39` — claim that rules "are not all
  resident" is FALSE (all 22 rule files are injected verbatim every session,
  ~10.3k words / ~14k tokens; confirmed by direct inspection). Fix: correct the
  wording to state rules ARE resident; pursue paths:-scoping only after the
  verification task below.
- [ ] `agents/**` (frontend-developer.md:23,147; java-architect.md:7,14;
  angular-architect, test-automator, devops-engineer, legacy-modernizer,
  mobile-developer, docker-expert, terraform-engineer, dotnet-core-expert) —
  coverage floors of 80-85% contradict `testing.md` 90/90/90 and none cite it.
  Fix: raise to 90/90/90 or add explicit per-agent rationale + a testing.md
  citation.
- [ ] `skills/fix/SKILL.md:86` — "skip re-diagnosis if the new error is
  straightforward" contradicts `diagnosis.md:33-34` "no fix before a stated
  mechanism." Fix: require a one-line mechanism even on iteration, or scope
  diagnosis.md's mandate to exclude fast-iterate fix loops.
- [ ] `agents/` (119 of 126 files) — no Required Rules block, and the "subagents
  must Read the rule file" instruction lives only in parallelism.md, which
  subagents don't auto-load (`grep "Read .*rules/" agents/` = 0). Fix: add a
  minimal Required Rules + "Read the file" line to the highest-traffic agents
  (reviewers, architects, language leads) — start there, not all 119.
- [ ] `settings.json` (Explore/Plan model) — v2.1.198 made Explore/Plan inherit
  the session model; with `model: fable`, every codebase search bills at Fable
  tier. Fix: add a user-level `Explore`/`Plan` agent with `model: haiku`
  (or `opus`=Opus 5 now). VERIFY the v2.1.198 inheritance behavior first.
- [ ] `skills/self-improve/SKILL.md:145-151` — effort table omits Fable 5 from
  every row (line 150 = "Opus 4.8, Opus 4.7, Sonnet 5"); Fable 5 supports the
  full low–max range. Fix: add Fable 5 (and Opus 5, per W0) to the effort rows.
- [ ] `rules/prompting-quality.md:52-55` — open `<!-- Code review (2026-07-01) -->`
  comment, 23 days unresolved, re: unverified `paths:`-scoped rule loading. Fix:
  WebFetch code.claude.com/docs/en/settings to confirm/deny paths: support, then
  resolve the comment. (Unblocks the prompting-quality.md:35-39 structural fix.)
- [ ] `settings.json` permissions — `cp`/`mv` absent from global allow despite
  being in every other settings file → constant prompts on basic file ops. Fix:
  add `Bash(cp:*)` and `Bash(mv:*)`.
- [ ] `skills/plan-mission/SKILL.md` — 5 sequential user-confirmation phases with
  NO resume/progress gate (self-improve/upgrade-deps/auth-setup have one). Fix:
  add a `.plan-mission-progress.md` gate mirroring self-improve's Phase 0.
- [ ] `skills/{self-improve,code-review,project-bootstrap}/SKILL.md` — phases gate
  on "all N agents complete" but handle only thin/partial responses, not a
  crashed/killed/timed-out agent (hit live this run when Agent A was killed).
  Fix: add "if an agent returns no output, relaunch once or proceed with an
  explicit gap note in the report."
- [ ] 13 skills (auth/analytics/compliance/payments/i18n/testing-setup,
  powerpoint-addin-setup, project-bootstrap, sandbox, file-organizer,
  internal-comms, video-downloader, doc-pdf, doc-xlsx) — carry a per-model
  routing line but never invoke the Agent tool. Fix: strip the vestigial routing
  line from the non-orchestrating skills.
- [ ] `.claude/settings.local.json` — disables the serena MCP server yet lists 11
  dead `mcp__serena__*` permissions and grants unscoped Read/Write/Edit/Glob/
  Grep(*). Fix: drop the dead serena perms; confirm the unscoped grants are
  intentional (global uses directory-scoped grants).
- [ ] `rules/prompting-quality.md:104-109` (+ `plan-mission/SKILL.md:412`,
  `self-improve/SKILL.md:243,804`) — states "Opus-tier models over-elaborate
  without explicit constraint" as established fact and escalates to a hard "must";
  arXiv:2604.00025 tested 31 open models 0.5B-405B, named no frontier model. Fix:
  restate as an observed-in-practice heuristic; keep the practice.
- [ ] `skills/compliance-setup/SKILL.md` — lacks the "verify against current docs"
  step its siblings (auth/payments/analytics-setup) have, despite integrating 4
  external services (Termly, Canny, SendGrid, R2). Fix: add a verify-against-docs
  step before wiring each service.

## Consider improving (Suggestion)
- [ ] `templates/autonomous-settings.json:2` — `"fallbackModel": "opus"` is a
  bare string; docs show array-chain syntax. Fix: verify bare-string acceptance
  or convert to `["opus","sonnet"]`.
- [ ] `skills/{self-improve,code-review,plan-mission}/SKILL.md` — 809/534/417
  lines, no `references/` dir (0 skills repo-wide have one). Fix: move stable
  rubric/template blocks to `references/` per the Agent Skills progressive-
  disclosure spec.
- [ ] `rules/parallelism.md` §6 — governs subagent return SHAPE but not SIZE.
  Fix: add "target 1k-2k token returns" (Anthropic subagent guidance).
- [ ] `skills/self-improve/SKILL.md:646-654` — duplicates code-review's scoring
  rubric (390-412) instead of referencing it; already drifted. Fix: reference,
  don't copy.
- [ ] `.gitignore` — add `daemon-auth-*` (daemon-auth-cooldown /
  daemon-auth-status.json are untracked and unignored; contents benign).
- [ ] `skills/self-improve/SKILL.md:678` verdict table — when Critical=0 AND
  Warning=0, both APPROVE and APPROVE-WITH-NITS clauses are satisfiable. Fix:
  make NITS require Warning≥1.
- [ ] `rules/parallelism.md:38-67` — "Agent prompt structure" is a 10-item
  parallel-prescriptive list over the file's own ≤6 budget. Fix: split into
  sub-sections or mark as an ordered procedure (procedures are exempt).
- [ ] Hook robustness — `notify-on-stop.sh:26-28` swallows notifier failures;
  `check-complexity.py:136-138` fail-open with no logging; `session-start.sh:29-37`
  async auto-install has no timeout. Fix: log on failure; add a timeout guard.
- [ ] `agents/04-quality-security/{code-reviewer,architect-reviewer}.md` — told to
  use `sg`/ast-grep but tool list denies Bash. Fix: grant scoped `Bash(sg:*)` or
  drop the ast-grep instruction.

## Inline comments to add (Notes)
- [ ] `agents/**` (44 files listing MultiEdit) — add comment:
  `<!-- Code review (2026-07-24): verify MultiEdit still resolves for background subagents (v2.1.198 backgrounds by default; docs allowlist shows Edit). Smoke-test one agent. -->`
- [ ] `agents/09-meta-orchestration/it-ops-orchestrator.md` — add comment:
  `<!-- Code review (2026-07-24): no Agent/Task tool in frontmatter — confirm it can actually route to specialists, else add the tool. -->`
- [ ] `CLAUDE.md:34-36` — add comment:
  `<!-- Code review (2026-07-24): this describes post-compact-context.md as 1 section / "not in any instruction file"; it has 4 sections, 3 of which condense rules/. Reword. -->`
- [ ] `post-compact-context.md` — add comment:
  `<!-- Code review (2026-07-24): restores how to RESUME a batch but not how to CLOSE one (between-batch quality gates + 2-tries stop, autonomous-execution.md:68-82). Add a condensed close-out line; fund from the commit-format overlap with CLAUDE.md:57. -->`

## Research-source follow-up
- [ ] `skills/self-improve/research-urls.md` — model doc pages returned stale
  (pre-Opus-5) content on 2026-07-24 despite 200 status. Not a dead URL, but the
  fetch-guard can't catch same-day staleness. Consider: on model-launch days,
  cross-check the models/overview fetch against a fresh WebSearch before trusting
  the "current model" list.
