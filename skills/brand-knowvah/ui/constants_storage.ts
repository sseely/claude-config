// Persistent localStorage keys used across the app.
// ADAPT: update LANG key prefix to match your app name (e.g. 'myapp_lang').
export const STORAGE_KEYS = {
  COLOR_MODE: 'colorMode',
  CONTRAST_MODE: 'contrastMode',
  SIDEBAR_COLLAPSED: 'sidebarCollapsed',
  LANG: 'knowvah_lang',
} as const;
