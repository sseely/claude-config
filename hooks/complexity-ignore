# Vendored / reference code we port but do not own — exempt from the
# complexity, file-length, and function-length checks. Their structure is
# upstream's choice and must be preserved (faithful-port discipline), so our
# quality bar does not apply. One path-prefix or glob per line; `~` and
# `$VARS` expand; lines starting with `#` are comments.

# PlantUML fork — upstream Java; we maintain only a tiny dot-output delta.
~/git/plantuml

# Graphviz C source — authoritative layout-algorithm reference.
~/git/graphviz

# Graphviz TypeScript port — consumed as a dependency, not owned here.
~/git/graphviz-ts
/Users/scottseely/git/plantuml-ts/src/diagrams/class/class-notes.ts

# ASCIIMath -> LaTeX converter, a faithful transcription of upstream's
# math/ASCIIMathTeXImg.java (1032 lines) and math/AsciiMath.java. Upstream's
# structure -- one long symbol table plus a recursive-descent parser whose
# functions exceed every limit -- must be preserved character for character;
# restructuring it would make divergences undetectable against the Java.
/Users/scottseely/git/knowvah/plantuml-ts/src/core/math/ASCIIMathTeXImg.ts
/Users/scottseely/git/knowvah/plantuml-ts/src/core/math/ASCIIMathTeXImgSymbols.ts
/Users/scottseely/git/knowvah/plantuml-ts/src/core/math/AsciiMath.ts

